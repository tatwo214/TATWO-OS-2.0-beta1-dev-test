# TATWO OS 憲法入口

TATWO OS 的憲法位於每台設備的入口 `~/AI/TATWO OS/os.md`，由主設備派發；安裝時由 App 內建範本建立。本 repo 不另立憲法。

## 發行通道摘要（依入口憲法 §9）

發行通道（2026-09-13）：公開版 `vX.Y.Z` 三段、私人版 `vX.Y.Z.NNN` 四段（NNN 從 001 遞增，是下一個公開版 `X.Y.(Z+1)` 的候選）；排序 `2.0.5 < 2.0.5.001 < 2.0.5.002 < 2.0.6`。每個改動先發私人版、內部裝置 soak；使用者授權後由 `scripts/promote-release.sh --i-authorize` 把最後一個候選同 commit 重打包成 `X.Y.(Z+1)` 公私同版，該列車即結束、舊候選標 pre-release，下一列車從 `X.Y.(Z+1).001` 重新起算。沒授權就不得進公開倉。

打包與發布在主設備進行；操作細節見 `$tatwo-os-update`。摘要與入口憲法衝突時，以入口憲法為準。
